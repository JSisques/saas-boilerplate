import { Bot, Code2, FileCode2, GitPullRequest, HelpCircle, Home, Link, Settings2, User } from 'lucide-react';

export const routes = {
  home: {
    path: '/',
    icon: Home,
    name: 'home',
  },
  settings: {
    path: '/settings',
    icon: Settings2,
    name: 'settings',
  },
  help: {
    path: 'https://docs.app.com',
    icon: HelpCircle,
    name: 'help',
  },
};
